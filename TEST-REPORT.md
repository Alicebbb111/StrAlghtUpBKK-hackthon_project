# Verification report · SkillBridge 2.0

ตรวจเมื่อ 14 กันยายน 2026 บน Windows และ Node.js v24.19.0

## Automated tests

คำสั่ง `npm test` — **10 tests passed, 0 failed**

| ตรวจอะไร | ผล |
|---|---|
| 3 tracks, 12 lessons, 36 assessment questions และไม่ส่งเฉลยใน catalog | ผ่าน |
| Guest → registration → login พร้อมความก้าวหน้าเดิม | ผ่าน |
| ตรวจคะแนนบน server ปฏิเสธคำตอบไม่ครบและการส่งซ้ำ | ผ่าน |
| โจทย์ครบทั้ง 12 บท และ completion ไม่เพิ่มซ้ำ | ผ่าน |
| แยกข้อมูลผู้ใช้ ป้องกันใช้ assessment ของผู้อื่น | ผ่าน |
| Validation, same-origin headers, email/password constraints | ผ่าน |
| URL หน้าเดิม, security headers และไม่เปิดเผยไฟล์ฐานข้อมูล/เซิร์ฟเวอร์ | ผ่าน |
| Skill analysis รองรับ 3 เส้นทางและตรวจขอบเขตคะแนน | ผ่าน |
| คำขอที่ส่งข้อมูลช้าพร้อมกันไม่เขียนทับความก้าวหน้า | ผ่าน |
| ปิดแล้วเปิดเซิร์ฟเวอร์ใหม่ ข้อมูลและ session ยังอยู่ | ผ่าน |

ชุดทดสอบใช้ฐานข้อมูลชั่วคราว ไม่แก้ฐานข้อมูลที่ใช้เปิดหน้า preview

## Browser verification

ใช้เบราว์เซอร์ในแอปเพื่อลองเส้นทางใช้งานจริง:

- เปิด Overview และคลิกเริ่มประเมิน
- ลองกด Next โดยไม่ตอบ ได้ข้อความให้เลือกคำตอบ
- เลือกคำตอบแล้วรีเฟรช คำตอบเดิมยังถูกเลือก
- ตอบครบ 12 ข้อ ได้ผล 9/12 ตามชุดคำตอบที่ทดสอบ พร้อมคะแนนแยกทักษะ
- เปิด learning path จากผลประเมิน แล้วเปิดบทเรียนแรก
- ลองตอบโจทย์ผิดและถูก ได้ feedback และสถานะ Completed ตามจริง
- บันทึกโน้ตภาษาไทยผสมอังกฤษ และ bookmark แล้วรีเฟรชตรวจว่ายังอยู่
- ค้นหา SQL ได้บทเรียน 1 บท และกรอง Saved ได้บทที่บันทึกไว้ 1 บท
- Global search ค้นหา CSS พบ Design for every screen
- Passport แสดงผลประเมินและบทเรียนที่จบจากการทดสอบ
- ปุ่ม JSON ส่งไฟล์ผ่าน Content-Disposition; เบราว์เซอร์รายงาน download event สำเร็จ
- ตรวจภาพ desktop และ mobile 390 × 844; ตรวจหน้า Overview ที่กว้าง 320 px แล้วไม่มี horizontal overflow
- เมนูมือถือเปิด/ปิดและนำทางได้
- ไม่พบ console error/warning ในการตรวจรอบสุดท้าย

ภาพใน `docs/previews/` ใช้ข้อมูลทดลองที่เกิดจากการคลิกจริง ไม่ใช่ข้อมูลที่เติมไว้ใน source package

## ขอบเขตการตรวจ

ตรวจปุ่ม Print / Save PDF และมี CSS สำหรับงานพิมพ์ แต่เบราว์เซอร์ทดสอบในแอปไม่ได้ให้หน้าต่างพิมพ์ของระบบ จึงไม่ได้ยืนยันไฟล์ PDF ที่บันทึกจาก Chrome/Edge จริง ผู้สาธิตควรเปิดผ่านเบราว์เซอร์ปกติแล้วเลือก Save as PDF ก่อนนำเสนอ

ทดสอบ authentication แบบครบวงจรผ่าน integration tests ไม่ได้ทดสอบผู้ให้บริการอีเมล เพราะไม่มี email verification/reset ในเวอร์ชันนี้ ไม่มี load test, penetration test, screen reader audit เต็มรูปแบบ หรือการทดสอบบนโทรศัพท์จริง

## Shipping

ไฟล์ส่งงานไม่รวม `data/`, `node_modules/`, `.env` หรือข้อมูลบัญชีที่ใช้ตรวจ หน้า preview ที่เปิดอยู่ใช้ data directory แยกไว้ต่างหาก โค้ดจะสร้างฐานข้อมูลว่างอัตโนมัติเมื่อผู้รับรันครั้งแรก
